import * as actions from '../const';

const action = (id) => {
  return {
    type: actions.TOP_MENU,
    id: id
  };
};

const action2 = (id) => {
  return {
    type: actions.TOP_MENU,
    id: id
  };
};
export default action;
